
<%= form_for(@post, :url => group_post_path(@post.group)) do |f| %>
    <div class="controls">
      <%= f.text_field :body, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
<% end %>