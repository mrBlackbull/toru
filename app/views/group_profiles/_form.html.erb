<%= form_for(@group_profile) do |f| %>
  <% if @group_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_profile.errors.count, "error") %> prohibited this group_profile from being saved:</h2>

      <ul>
      <% @group_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
